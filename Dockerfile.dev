FROM node:18-slim

WORKDIR /src

# Install dependencies first
COPY package*.json ./
RUN npm install

# Copy source code (optional, Compose will mount anyway)
COPY . .

# Expose container port
EXPOSE 3000

CMD ["npm", "run", "dev"]
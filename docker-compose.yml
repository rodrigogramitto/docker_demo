version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/src/src        # host src maps directly into container src folder
      - /src/node_modules     # keep container deps intact

